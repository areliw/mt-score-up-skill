@AGENTS.md

<!--
Source of truth = AGENTS.md (imported above). Gemini CLI reads GEMINI.md by default
and expands @-imports, so this keeps them in sync. Put project rules in AGENTS.md only.
(Alternatively, Gemini CLI can read AGENTS.md directly via settings.json
context.fileName — this import needs no config and is the portable default.)
-->
